---
import BaseHead from '../components/BaseHead.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';

const heroVideo = 'https://interactive-examples.mdn.mozilla.net/media/cc0-videos/flower.mp4';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<main class="page">
			<section class="hero" id="top">
				<p class="hero-kicker">Journal</p>
				<h1 class="hero-title">retired at 33.</h1>
				<p class="hero-subtitle">
					A monochrome archive of decisions, discipline, and the slow craft of living well.
				</p>
			</section>

			<nav class="primary-nav" aria-label="Primary">
				<a href="#connect">Connect</a>
				<a href="#call">Call</a>
				<a href="#book">Book</a>
				<a href="#videos">Videos</a>
				<a href="#charity">Charity</a>
			</nav>

			<section class="feature-video" aria-label="Featured film">
				<div class="video-frame">
					<video
						id="hero-video"
						class="hero-video"
						autoplay
						playsinline
						loop
						muted
						preload="metadata"
					>
						<source src={heroVideo} type="video/mp4" />
					</video>
					<button class="audio-toggle" type="button" aria-pressed="false">Audio: Low</button>
				</div>
				<p class="video-caption">A quiet, steady reel that sets the pace for the journal.</p>
			</section>

			<section id="connect" class="section">
				<div class="section-header">
					<p class="section-kicker">Connect</p>
					<h2>Private notes for thoughtful collaborations.</h2>
				</div>
				<div class="section-body">
					<p>
						For brand partnerships, editorial collaborations, or long-term projects, reach out with
						context and intent. Every inquiry is read with care.
					</p>
					<ul class="section-list">
						<li>Editorial features, interviews, and guest essays.</li>
						<li>Brand strategy sessions and narrative positioning.</li>
						<li>Quiet launches and discreet advisory support.</li>
					</ul>
					<a class="section-link" href="mailto:hello@retired33.com">Start the conversation</a>
				</div>
			</section>

			<section id="call" class="section">
				<div class="section-header">
					<p class="section-kicker">Call</p>
					<h2>Schedule a short, focused call.</h2>
				</div>
				<div class="section-body">
					<p>
						Select a time for a 20-minute check-in. We keep the agenda tight, the outcomes clear, and
						the next steps immediate.
					</p>
					<ul class="section-list">
						<li>Availability across CET, EST, and PST.</li>
						<li>Pre-call notes to keep the conversation sharp.</li>
						<li>Follow-up summary within 24 hours.</li>
					</ul>
					<a class="section-link" href="#book">Reserve a slot</a>
				</div>
			</section>

			<section id="book" class="section">
				<div class="section-header">
					<p class="section-kicker">Book</p>
					<h2>Secure a personal session or advisory day.</h2>
				</div>
				<div class="section-body">
					<p>
						Booking is curated and limited. Each engagement includes a discovery brief, an action plan,
						and a tailored summary.
					</p>
					<ul class="section-list">
						<li>1:1 sessions for founders and creatives.</li>
						<li>Half-day advisory for executive teams.</li>
						<li>Review of brand, product, and positioning.</li>
					</ul>
					<a class="section-link" href="mailto:book@retired33.com">Request availability</a>
				</div>
			</section>

			<section id="videos" class="section">
				<div class="section-header">
					<p class="section-kicker">Videos</p>
					<h2>Short films and long-form conversations.</h2>
				</div>
				<div class="section-body">
					<p>
						A steady stream of clean, monochrome visuals — craft, collection stories, and candid
						conversations with builders.
					</p>
					<ul class="section-list">
						<li>Studio walkthroughs and quiet desk rituals.</li>
						<li>Micro-documentaries on enduring design.</li>
						<li>Weekly video dispatches with written notes.</li>
					</ul>
					<a class="section-link" href="#top">View the archive</a>
				</div>
			</section>

			<section id="charity" class="section">
				<div class="section-header">
					<p class="section-kicker">Charity</p>
					<h2>Giving quietly, consistently, and with purpose.</h2>
				</div>
				<div class="section-body">
					<p>
						Each quarter, a portion of project revenue supports education and craft preservation
						initiatives. Transparency is shared in the annual report.
					</p>
					<ul class="section-list">
						<li>Scholarships for emerging designers.</li>
						<li>Tool grants for independent makers.</li>
						<li>Community-led mentorship programs.</li>
					</ul>
					<a class="section-link" href="mailto:charity@retired33.com">Partner with the fund</a>
				</div>
			</section>

			<footer class="site-footer">Copyright 2026 Retired at 33. All rights reserved.</footer>
		</main>

		<script>
			const heroVideo = document.getElementById('hero-video');
			const audioToggle = document.querySelector('.audio-toggle');

			if (heroVideo) {
				const updateAudioLabel = () => {
					if (!audioToggle) return;
					audioToggle.setAttribute('aria-pressed', String(!heroVideo.muted));
					audioToggle.textContent = heroVideo.muted ? 'Audio: Muted' : 'Audio: Low';
				};

				const attemptAutoplay = async () => {
					heroVideo.muted = false;
					heroVideo.volume = 0.2;
					try {
						await heroVideo.play();
					} catch (error) {
						heroVideo.muted = true;
						heroVideo.volume = 0.2;
						updateAudioLabel();
						heroVideo.play().catch(() => undefined);
					}
				};

				attemptAutoplay();
				updateAudioLabel();

				audioToggle?.addEventListener('click', () => {
					heroVideo.muted = !heroVideo.muted;
					if (!heroVideo.muted) {
						heroVideo.volume = 0.2;
					}
					updateAudioLabel();
					if (heroVideo.paused) {
						heroVideo.play().catch(() => undefined);
					}
				});
			}
		</script>

		<style>
			.page {
				display: flex;
				flex-direction: column;
				gap: 3rem;
			}

			.hero {
				text-align: center;
				margin-top: 1rem;
				display: flex;
				flex-direction: column;
				gap: 1rem;
			}

			.hero-kicker {
				text-transform: uppercase;
				letter-spacing: 0.45em;
				font-size: 0.7rem;
				font-weight: 600;
				margin: 0;
			}

			.hero-title {
				font-size: clamp(2.8rem, 5vw, 5rem);
				font-weight: 700;
				margin: 0;
			}

			.hero-subtitle {
				font-size: clamp(1rem, 1.8vw, 1.2rem);
				font-weight: 300;
				max-width: 720px;
				margin: 0 auto;
				color: rgb(var(--gray-dark));
			}

			.primary-nav {
				display: flex;
				justify-content: center;
				align-items: center;
				gap: 2.5rem;
				flex-wrap: wrap;
				padding: 1.2rem 0;
				border-top: 1px solid rgb(var(--black));
				border-bottom: 1px solid rgb(var(--black));
				text-transform: uppercase;
				letter-spacing: 0.25em;
				font-size: 0.75rem;
				font-weight: 600;
			}

			.primary-nav a {
				position: relative;
				padding: 0.2rem 0;
			}

			.primary-nav a::after {
				content: '';
				position: absolute;
				left: 0;
				bottom: -0.4rem;
				height: 1px;
				width: 100%;
				background: rgb(var(--black));
				transform: scaleX(0);
				transform-origin: right;
				transition: transform 0.3s ease;
			}

			.primary-nav a:hover::after,
			.primary-nav a:focus-visible::after {
				transform: scaleX(1);
				transform-origin: left;
			}

			.feature-video {
				display: flex;
				flex-direction: column;
				gap: 1rem;
			}

			.video-frame {
				position: relative;
				border: 1px solid rgb(var(--black));
				overflow: hidden;
				background: rgb(var(--black));
			}

			.hero-video {
				width: 100%;
				height: min(65vh, 560px);
				object-fit: cover;
				display: block;
			}

			.audio-toggle {
				position: absolute;
				right: 1.25rem;
				bottom: 1.25rem;
				background: rgba(255, 255, 255, 0.85);
				border: 1px solid rgb(var(--black));
				color: rgb(var(--black));
				font-family: inherit;
				font-size: 0.7rem;
				text-transform: uppercase;
				letter-spacing: 0.2em;
				padding: 0.6rem 1rem;
				cursor: pointer;
			}

			.video-caption {
				font-size: 0.95rem;
				font-weight: 400;
				margin: 0;
				color: rgb(var(--gray-dark));
				text-align: center;
			}

			.section {
				display: grid;
				grid-template-columns: minmax(0, 1fr) minmax(0, 1.2fr);
				gap: 2.5rem;
				padding-top: 2.5rem;
				border-top: 1px solid rgb(var(--black));
				scroll-margin-top: 2rem;
			}

			.section-kicker {
				text-transform: uppercase;
				letter-spacing: 0.3em;
				font-size: 0.7rem;
				font-weight: 600;
				margin: 0;
			}

			.section-body {
				display: flex;
				flex-direction: column;
				gap: 1.2rem;
			}

			.section-body p {
				margin: 0;
			}

			.section-list {
				list-style: none;
				padding: 0;
				margin: 0;
				display: grid;
				gap: 0.9rem;
			}

			.section-list li {
				padding-top: 0.75rem;
				border-top: 1px solid rgb(var(--gray-light));
				font-weight: 500;
			}

			.section-link {
				align-self: flex-start;
				text-transform: uppercase;
				letter-spacing: 0.25em;
				font-size: 0.72rem;
				font-weight: 600;
				padding-bottom: 0.4rem;
				border-bottom: 1px solid rgb(var(--black));
			}

			.site-footer {
				padding-top: 2.5rem;
				border-top: 1px solid rgb(var(--black));
				text-align: center;
				font-size: 0.7rem;
				letter-spacing: 0.3em;
				text-transform: uppercase;
				font-weight: 600;
			}

			@media (max-width: 900px) {
				.section {
					grid-template-columns: 1fr;
				}
			}

			@media (max-width: 600px) {
				.primary-nav {
					gap: 1.5rem;
					letter-spacing: 0.18em;
				}

				.audio-toggle {
					right: 0.75rem;
					bottom: 0.75rem;
				}
			}
		</style>
	</body>
</html>
